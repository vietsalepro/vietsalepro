## ADDED Requirements

### Requirement: Helper functions + custom fetch wrapper

The system MUST complete the implementation for: `current_tenant_id()` từ header, `lib/tenant.ts`, `lib/supabase.ts` inject header..

#### Scenario: Acceptance criteria 1
- **GIVEN** the system is in the required state
- **WHEN** Header `x-tenant-id` được gửi kèm request.
- **THEN** the expected outcome is achieved

## MODIFIED Requirements

- None outside this sub-phase.

## REMOVED Requirements

- None outside this sub-phase.
