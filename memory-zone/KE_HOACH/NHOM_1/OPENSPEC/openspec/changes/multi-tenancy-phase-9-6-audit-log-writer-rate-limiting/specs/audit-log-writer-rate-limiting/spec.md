## ADDED Requirements

### Requirement: `audit-log-writer` + rate limiting

The system MUST complete the implementation for: ghi audit log và thiết lập rate limiting..

#### Scenario: Acceptance criteria 1
- **GIVEN** the system is in the required state
- **WHEN** Rate limiting chặn brute-force login và spam tạo tenant.
- **THEN** the expected outcome is achieved

## MODIFIED Requirements

- None outside this sub-phase.

## REMOVED Requirements

- None outside this sub-phase.
