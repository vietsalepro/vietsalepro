## ADDED Requirements

### Requirement: p7-1-billing-schema-bank-config
The system MUST implement p7.1 — billing schema + bank/company config: invoices, invoice_items, payments, bank_accounts..

#### Scenario: p7-1-billing-schema-bank-config happy path
- **GIVEN** the system admin is authenticated and the prerequisite phases are complete
- **WHEN** the system admin performs the P7 1 Billing Schema Bank Config actions
- **THEN** the system applies the changes correctly and returns the expected data

## MODIFIED Requirements

<!-- Only if existing capabilities are changing. -->

## REMOVED Requirements

<!-- Only if deprecating features. -->
