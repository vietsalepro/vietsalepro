## ADDED Requirements

### Requirement: p13-1-system-health
The system MUST implement p13.1 — system health dashboard: db/storage/edge function status cards..

#### Scenario: p13-1-system-health happy path
- **GIVEN** the system admin is authenticated and the prerequisite phases are complete
- **WHEN** the system admin performs the P13 1 System Health actions
- **THEN** the system applies the changes correctly and returns the expected data

## MODIFIED Requirements

<!-- Only if existing capabilities are changing. -->

## REMOVED Requirements

<!-- Only if deprecating features. -->
