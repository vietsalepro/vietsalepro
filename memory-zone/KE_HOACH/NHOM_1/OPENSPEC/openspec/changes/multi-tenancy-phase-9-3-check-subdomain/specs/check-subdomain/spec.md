## ADDED Requirements

### Requirement: `check-subdomain`

The system MUST complete the implementation for: kiểm tra subdomain có sẵn..

#### Scenario: Acceptance criteria 1
- **GIVEN** the system is in the required state
- **WHEN** Subdomain tồn tại → trả về `false`
- **THEN** the expected outcome is achieved

#### Scenario: Acceptance criteria 2
- **GIVEN** the system is in the required state
- **WHEN** Subdomain trống → trả về `true`
- **THEN** the expected outcome is achieved

## MODIFIED Requirements

- None outside this sub-phase.

## REMOVED Requirements

- None outside this sub-phase.
