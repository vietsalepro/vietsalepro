## ADDED Requirements

### Requirement: Config & misc tables

The system MUST complete the implementation for: config & misc tables.

#### Scenario: Acceptance criteria 1
- **GIVEN** the system is in the required state
- **WHEN** Các bảng config đã có cột `tenant_id` và FK.
- **THEN** the expected outcome is achieved

#### Scenario: Acceptance criteria 2
- **GIVEN** the system is in the required state
- **WHEN** `types.ts` không còn lỗi TS.
- **THEN** the expected outcome is achieved

## MODIFIED Requirements

- None outside this sub-phase.

## REMOVED Requirements

- None outside this sub-phase.
