## ADDED Requirements

### Requirement: p7-3-payment-confirm-lifecycle
The system MUST implement p7.3 — payment confirmation + invoice lifecycle (pending/paid/overdue/cancelled/expired)..

#### Scenario: p7-3-payment-confirm-lifecycle happy path
- **GIVEN** the system admin is authenticated and the prerequisite phases are complete
- **WHEN** the system admin performs the P7 3 Payment Confirm Lifecycle actions
- **THEN** the system applies the changes correctly and returns the expected data

## MODIFIED Requirements

<!-- Only if existing capabilities are changing. -->

## REMOVED Requirements

<!-- Only if deprecating features. -->
