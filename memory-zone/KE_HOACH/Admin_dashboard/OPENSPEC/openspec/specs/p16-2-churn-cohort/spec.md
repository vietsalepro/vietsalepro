# p16-2-churn-cohort Specification

## Purpose
TBD - created by archiving change admin-dashboard-p16-2-churn-cohort. Update Purpose after archive.
## Requirements
### Requirement: p16-2-churn-cohort
The system MUST implement p16.2 — churn + cohort + tenant ltv + sales funnel + charts (yagni)..

#### Scenario: p16-2-churn-cohort happy path
- **GIVEN** the system admin is authenticated and the prerequisite phases are complete
- **WHEN** the system admin performs the P16 2 Churn Cohort actions
- **THEN** the system applies the changes correctly and returns the expected data

