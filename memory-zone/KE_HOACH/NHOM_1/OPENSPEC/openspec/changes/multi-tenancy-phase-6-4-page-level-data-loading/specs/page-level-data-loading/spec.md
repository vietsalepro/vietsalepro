## ADDED Requirements

### Requirement: Page-level data loading

The system MUST complete the implementation for: sửa các page cần load data để nhận `tenantId` từ `TenantContext`..

#### Scenario: Acceptance criteria 1
- **GIVEN** the system is in the required state
- **WHEN** Mỗi page chỉ hiển thị dữ liệu của tenant hiện tại.
- **THEN** the expected outcome is achieved

## MODIFIED Requirements

- None outside this sub-phase.

## REMOVED Requirements

- None outside this sub-phase.
