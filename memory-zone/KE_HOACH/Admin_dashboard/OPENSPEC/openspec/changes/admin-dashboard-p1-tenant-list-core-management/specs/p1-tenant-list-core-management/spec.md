## ADDED Requirements

### Requirement: p1-tenant-list-core-management
The system MUST implement p1 — tenant list & core management: search/filter/pagination, soft delete, kpi cards..

#### Scenario: p1-tenant-list-core-management happy path
- **GIVEN** the system admin is authenticated and the prerequisite phases are complete
- **WHEN** the system admin performs the P1 Tenant List Core Management actions
- **THEN** the system applies the changes correctly and returns the expected data

## MODIFIED Requirements

<!-- Only if existing capabilities are changing. -->

## REMOVED Requirements

<!-- Only if deprecating features. -->
