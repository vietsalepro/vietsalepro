## ADDED Requirements

### Requirement: p7-2-invoice-create-pricing
The system MUST implement p7.2 — rpc create invoice + auto numbering + pricing (monthly/yearly/prepaid) asia/ho_chi_minh..

#### Scenario: p7-2-invoice-create-pricing happy path
- **GIVEN** the system admin is authenticated and the prerequisite phases are complete
- **WHEN** the system admin performs the P7 2 Invoice Create Pricing actions
- **THEN** the system applies the changes correctly and returns the expected data

## MODIFIED Requirements

<!-- Only if existing capabilities are changing. -->

## REMOVED Requirements

<!-- Only if deprecating features. -->
